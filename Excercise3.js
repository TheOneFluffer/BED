//Exercise 3
function  multiplicationTable(length)
{
    for(i = 0; i <= length; i++)
    {
        myArr.push[i];
        for(j = 0; j <= length; j++)
        {
            console.log()
        }
    }
}

console.log(multiplicationTable(12));